# MySQL 数据库快速初始化

## 🚀 一键初始化（推荐）

```bash
cd nuxtjs/scripts
chmod +x init-database.sh
sudo ./init-database.sh
```

## 📝 手动初始化步骤

### 1. 安装 MySQL

```bash
# Ubuntu/Debian
sudo apt update && sudo apt install mysql-server -y
sudo systemctl start mysql && sudo systemctl enable mysql

# CentOS/RHEL
sudo yum install mysql-server -y
sudo systemctl start mysqld && sudo systemctl enable mysqld
```

### 2. 设置 root 密码为 123456

```bash
sudo mysql
```

```sql
ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY '123456';
FLUSH PRIVILEGES;
EXIT;
```

### 3. 配置远程连接

```bash
# 编辑配置文件
sudo nano /etc/mysql/mysql.conf.d/mysqld.cnf
# 或
sudo nano /etc/my.cnf
```

修改或添加：
```ini
bind-address = 0.0.0.0
```

### 4. 创建远程用户并授权

```bash
mysql -u root -p123456
```

```sql
CREATE USER 'root'@'%' IDENTIFIED BY '123456';
GRANT ALL PRIVILEGES ON *.* TO 'root'@'%' WITH GRANT OPTION;
FLUSH PRIVILEGES;
CREATE DATABASE objct_b CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
EXIT;
```

### 5. 重启 MySQL

```bash
sudo systemctl restart mysql
# 或
sudo systemctl restart mysqld
```

### 6. 配置防火墙

```bash
# Ubuntu/Debian
sudo ufw allow 3306/tcp

# CentOS/RHEL
sudo firewall-cmd --permanent --add-service=mysql
sudo firewall-cmd --reload
```

### 7. 配置项目环境变量

在 `nuxtjs/.env` 文件中添加：

```env
DATABASE_URL="mysql://root:123456@你的服务器IP:3306/objct_b?charset=utf8mb4"
```

### 8. 运行数据库迁移

```bash
cd nuxtjs
npm run db:generate
npm run db:migrate
```

## ✅ 验证连接

```bash
# 测试本地连接
mysql -u root -p123456

# 测试远程连接（从其他机器）
mysql -h 你的服务器IP -u root -p123456
```

## 🔒 安全提醒

1. **云服务器**：在控制台安全组中开放 3306 端口
2. **生产环境**：建议创建专用数据库用户，不要使用 root
3. **密码强度**：生产环境使用更强的密码

## 📚 详细文档

查看 `数据库初始化指南.md` 获取完整说明和故障排查。

