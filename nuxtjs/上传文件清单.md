# 📦 本地打包后需要上传的文件清单

## ✅ 必须上传的文件/文件夹

### 1. `.output/` - **最重要！包含所有构建产物**
```
.output/
├── server/              # 服务端代码
│   ├── index.mjs        # 服务器入口文件（PM2 需要这个）
│   ├── node_modules/    # 生产依赖（已包含，不需要单独安装）
│   └── ...
├── public/              # 静态资源文件
└── nitro.json           # Nitro 配置
```

### 2. `package.json` - 依赖声明文件
- 服务器需要查看依赖信息
- Prisma 等工具需要这个文件

### 3. `prisma/` - 数据库相关（如果使用了数据库）
```
prisma/
├── schema.prisma        # 数据库 schema
└── migrations/          # 数据库迁移文件
```

## ❌ 不需要上传的文件

- ❌ `node_modules/` - .output/server/node_modules 已包含生产依赖
- ❌ `.env` - 环境变量文件（在服务器上单独创建）
- ❌ 源代码文件（.vue, .ts, .js 等）- 已打包在 .output 中
- ❌ `.nuxt/`, `.cache/`, `.data/` - 开发时的临时文件
- ❌ `package-lock.json` - 可选，但建议上传（用于版本锁定）

## 📋 快速检查清单

在提交前，确保以下文件存在：

```bash
# 检查 .output 是否存在
ls -la .output/

# 检查 package.json
ls -la package.json

# 检查 prisma（如果使用）
ls -la prisma/schema.prisma
```

## 🚀 Git 提交命令

```bash
# 1. 确保已构建
npm run build

# 2. 添加文件（使用 -f 强制添加，因为 .output 在 .gitignore 中）
git add -f .output
git add package.json
git add prisma  # 如果使用数据库

# 3. 提交
git commit -m "chore: 添加构建产物用于部署"

# 4. 推送
git push origin main
```

## 💡 提示

1. **.output 文件大小**：通常为 10-100MB，确保 git 仓库可以接受
2. **分支策略**：建议使用单独的部署分支（如 `deploy`）来提交构建产物
3. **服务器 Node.js 版本**：确保服务器 Node.js 版本 ≥ 18（Nuxt 3 要求）

